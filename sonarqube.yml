- name: Sample Playbook
  hosts:
    - localhost
  #hosts: all
  tasks:
    - name: "Create SonarQube system user"
      user:
        name: "sonar"
#        group: "{{ sonar_group }}"
        createhome: no
#        shell: "/sbin/nologin"
        comment: "SonarQube System user"
        state: present
